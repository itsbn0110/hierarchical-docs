export enum NodeType {
  FOLDER = 'folder',
  DOCUMENT = 'document',
}

export enum PermissionType {
  VIEWER = 'Viewer',
  EDITOR = 'Editor',
  OWNER = 'Owner',
}

export enum RequestStatus {
  PENDING = 'Pending',
  APPROVED = 'Approved',
  DENIED = 'Denied',
}
